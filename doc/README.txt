To run the model:
1. Run GAMA 1.8 
2. Import the folder Group18_FinalProject_2019-12-24 as a new GAMA project (right click User Models).
3. Double click the desired model to run (Battlefield), and then press BATTLEFIELD (green, top) to run the simulation.